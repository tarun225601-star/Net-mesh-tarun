plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
}

android {
    namespace = "com.netmesh.vpn"
    compileSdk = 34

    defaultConfig {
        applicationId = "com.netmesh.vpn"
        minSdk = 26           // Android 8 — ParcelFileDescriptor.fromFd, FileChannel NIO
        targetSdk = 34
        versionCode = 1
        versionName = "1.0.0"
    }

    buildFeatures {
        viewBinding = true
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }

    buildTypes {
        release {
            isMinifyEnabled = false
            proguardFiles(getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro")
        }
    }
}

dependencies {
    // WebRTC — Google's official pre-built AAR for Android
    implementation("io.github.webrtc-sdk:android:125.6422.06.1")

    // WebSocket client for signaling
    implementation("com.squareup.okhttp3:okhttp:4.12.0")

    // JSON parsing for signaling messages
    implementation("org.json:json:20240303")

    // Kotlin coroutines
    implementation("org.jetbrains.kotlinx:kotlinx-coroutines-android:1.8.1")

    // Lifecycle (ViewModel, lifecycleScope)
    implementation("androidx.lifecycle:lifecycle-runtime-ktx:2.7.0")
    implementation("androidx.activity:activity-ktx:1.9.0")
    implementation("androidx.appcompat:appcompat:1.7.0")

    // Material design components
    implementation("com.google.android.material:material:1.12.0")

    // LocalBroadcastManager — used to send state updates from Service to Activity
    implementation("androidx.localbroadcastmanager:localbroadcastmanager:1.1.0")

    // ConstraintLayout — used in activity_main.xml
    implementation("androidx.constraintlayout:constraintlayout:2.1.4")
}
